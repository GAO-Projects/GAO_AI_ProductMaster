// This file can be used for non-sensitive, client-side configuration.
// Secrets like API keys or passwords should not be stored here.
// Instead, they should be provided as environment variables in your deployment environment.
// For example, to set the admin password, you would set an environment variable
// named ADMIN_PASSWORD in your hosting provider's settings.

window.APP_CONFIG = {
  // Example configuration:
  // THEME: "dark",
};
